[CRITICAL] frontend/src/main.ts:11-29; backend/src/routes/auth.ts:5-25 — Flow autentikasi opsional dilanggar: frontend selalu memaksa semua halaman ke `/auth`, sementara backend tetap mengeluarkan token walau `APP_PASSWORD` kosong. Ini bertentangan langsung dengan RULES bahwa app harus bisa diakses tanpa login saat password tidak diisi. — Tambahkan status `auth_enabled` dari backend/config bootstrap, nonaktifkan route guard saat auth off, dan buat `/api/auth` menolak/disable login saat `APP_PASSWORD` kosong.

[WARNING] frontend/src/views/QuizView.vue:231-250 — `loadTopicInfo()` memanggil `fetchTopics(getTopicId())`, padahal API itu butuh `subject_id`, bukan `topic_id`. Akibatnya label topik dan tombol balik sering salah, lalu ditutup dengan fallback rapuh. — Sediakan endpoint/topic lookup yang benar (`GET /api/topics/:id` atau kirim subject context lewat router) lalu hapus lookup berbasis asumsi ini.

[WARNING] frontend/src/views/QuizView.vue:239-240 — Fallback `fetchTopics(1), fetchTopics(2), fetchTopics(3), fetchTopics(4)` meng-hardcode hanya 4 subject ID. Begitu seed berubah, ID tidak berurutan, atau subject bertambah, halaman quiz gagal memetakan topik. — Jangan enumerate ID tetap; ambil `subjectId` dari route state atau cari topik lewat endpoint spesifik berdasarkan `topic_id`.

[WARNING] backend/src/routes/questions.ts:17-19,87-125; backend/src/lib/scoring.ts:29-41 — Validasi jawaban terlalu longgar. `single_choice`/`true_false` menerima banyak pilihan lalu hanya membaca elemen pertama, dan `multiple_response` menerima duplikasi key karena `Set` menghapus duplikat diam-diam. Client yang rusak atau request manual bisa menghasilkan scoring yang ambigu. — Validasi `selected_keys` berdasarkan tipe soal: harus unik, harus subset dari option yang ada, dan untuk tipe single hanya boleh tepat 1 item.

[WARNING] backend/src/lib/auth-store.ts:1-15 — Token auth disimpan selamanya dalam `Set` tanpa TTL, revocation, atau cleanup. Pada proses yang hidup lama, memori akan terus bertambah dan token lama tetap valid sampai server restart. — Simpan metadata `issuedAt`, terapkan expiry pendek, dan bersihkan token kadaluarsa saat create/validate; jika perlu tambahkan endpoint logout.

[WARNING] backend/src/routes/questions.ts:56-61 — `ORDER BY RAND()` untuk mengambil soal acak akan makin mahal saat tabel `questions` membesar karena database harus menghitung random untuk banyak baris setiap request. — Ganti dengan strategi yang skalabel: random offset dari `COUNT(*)`, precomputed random key, atau sampling berdasarkan ID.

[WARNING] backend/src/__tests__; frontend/src/__tests__ — Coverage test tidak memenuhi RULES. Tidak ada route test backend (`subjects/topics/questions`) dan tidak ada view test frontend (`HomeView/TopicView/QuizView`), sehingga bug integrasi seperti auth opsional dan lookup topik lolos walau suite saat ini hijau. — Tambahkan test API Hono untuk semua endpoint utama dan test view untuk alur auth/home/topic/quiz.

[SUGGESTION] backend/src/lib/seed.ts:13-39,80-109 — Seed hanya memvalidasi bentuk dasar JSON, belum menegakkan invariant domain: `single_choice` harus tepat 1 jawaban benar, `multiple_response` minimal 2 jawaban benar, `true_false` harus tepat dua opsi `true/false`. Data seed yang salah bisa masuk dan merusak perilaku runtime. — Tambahkan `superRefine` pada schema Zod untuk aturan per tipe soal sebelum insert ke database.
